# Machine-local Claude Code settings (permissions, etc.) — never commit
.claude/settings.local.json

# Subagent-driven-development scratch (progress ledger, review notes) — never commit
.superpowers/
